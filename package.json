{
  "name": "todmorden-ferns-map",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build-geojson": "node scripts/build-geojson.js",
    "dev": "npm run build-geojson && vite",
    "build": "npm run build-geojson && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "proj4": "^2.9.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0",
    "shapefile": "^0.6.6"
  }
}
